tenets:
  - name: wrap_errs
    flows:
      codelingo/rewrite:
        newCode: some code
      codelingo/review:
    query: |
      import (
        codelingo/ast/go
      )

      go.func_decl(depth = any):
        @rewrite ""
        go.ident
 
            


